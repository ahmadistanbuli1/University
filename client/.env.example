# Leave empty for local dev (Vite proxies /api and /uploads to http://localhost:4000).
# Leave empty on Vercel when using vercel.json rewrites (same-origin /api proxy).
# Set only if the frontend calls the API host directly (not recommended for production).
VITE_API_URL=
